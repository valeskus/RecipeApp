export enum Difficulty {
    hard = 2,
    normal = 1,
    easy = 0
}

export enum Units {
    ML = 'ml',
    G = 'g'
}

export enum TagsUpdateStrategy {
    'replace' = 'replace',
    'merge' = 'merge'
}
