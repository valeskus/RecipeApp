import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonBarContainer: {
    flex: 1,
    flexDirection: 'row',
  },
  cardFormButtonContainer: {
    flex: 2,
    alignItems: 'flex-end',
  },
});
