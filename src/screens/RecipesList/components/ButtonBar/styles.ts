import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonBarContainer: {
    width: '100%',
    flexDirection: 'row',
  },
  cardFormButtonContainer: {
    flex: 2,
    alignItems: 'flex-end',
  },
});
